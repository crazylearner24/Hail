# Hail (scaffold)
Open in Android Studio (Ladybug or newer, JDK 17), let Gradle sync, then Run or `./gradlew assembleDebug`.
Search the code for `TODO` — hosts, request/response fields and auth header format that were not confirmed in your spec are left unguessed.
